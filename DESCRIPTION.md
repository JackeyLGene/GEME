GEME (Generative Economy Memory Entity) is a minimal computational system built on three axioms — competitive merging, adaptive forgetting, and self-referential observation — that produces an emergent six-layer cognitive architecture from a running frame economy. Operating with only three structural constants (δ, γ, τ) from which all behavioral thresholds derive, GEME demonstrates that self-referential operations carry near-zero mutual information with the input channel (I = 0.026 bits), self-reference and induction are structurally equivalent (Q + G ≡ PA), and frame count converges to stable attractors under economic pressure. The model is fully reproducible, requires zero external dependencies, and runs in ~10 seconds.
