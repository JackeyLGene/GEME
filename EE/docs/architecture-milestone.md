# Architecture Milestone: Time-Scale Recursion (M1.9)

**日期：** 2026-05-18 06:46
**状态：** 架构理解闭合，EE 就是递归的 BGM
**前置：** M1 (Geruon 内核)、G 句子实验 (E1-E7)、BGM GI=4 发现

---

## 1. 核心主张

**EE 不是发明新组件——EE 是递归应用 BGM 的时间解耦操作。**

BGM 已经完成了核心发现：
  - GEME 单元（棱镜）：如实呈现结构，无时间
  - G0（观察者）：以 GI=4 的间隔读取单元，添加 τ
  - 时间解耦增强分化 +49%（GI=4 帕累托最优）

EE 的任务：**在同一架构上，递归一层**——让 G0 的输出进入另一个以 GI=4 观察它的单元。重复此操作 N 次。信息质量在此递归中按 GI 的幂次积累。

### 1.1 问题诊断

Geruon 的错误不是"合并了棱镜和观察者"——它错误地把 G0 的 τ 放进了棱镜的帧经济中。正确的做法：

- 棱镜（GEME 单元）：帧经济无 τ，struct_sig 无 tau_bin，arrow_output 不含相位
- 观察者（G0）：以 GI=4 间隔读取棱镜输出，添加 τ，添加相位
- 元观察者（G0'）：以 GI²=16 间隔读取 G0 输出，监督代际关系

这是 BGM 架构的直接递归——不是新型组件。

### 1.2 G 句子实验的诊断映射

| 问题 | 根因 | 架构层 |
|------|------|--------|
| MI 始终 < 0.04 | 观察者的预测噪声干扰了棱镜的共现结构 | 棱镜-观察者未分离 |
| doubt 从未触发 | doubt 在棱镜内——当桥关闭时预测停止，doubt 无法激活 | 观察者被困在棱镜内 |
| 边界帧与桥重开时序模糊 | 边界帧可能是棱镜效果（结构）或观察者效果（认知） | 不可归因 |
| τ-bin 双峰有意义但无效 | τ 分化在棱镜内——它应该发生在观察者中 | 错误层 |

---

## 2. 时间尺度递归架构

### 2.1 递归模式

```
深度 0 (GEME):  帧经济 ←  环境输入
                 无 τ，无观察者，如实呈现结构
                 σ 签名：不含 tau_bin

深度 1 (G0):    读取深度 0 的帧输出
                 以 GI=4 间隔采样
                 添加 τ、预测引擎、相位循环
                 输出：时间化帧摘要

深度 2 (G0'):   读取深度 1 的 τ 轨迹
                 以 GI²=16 间隔采样
                 碰数检测、代际监督
                 输出：信息质量报告（SR-eff）

深度 N (G0^N):  读取深度 N-1 的输出
                 以 GI^N 间隔采样
                 信息质量在 GI 的幂次上积累
```

### 2.2 职责分层

| 操作 | 深度 | 时间尺度 |
|------|------|---------|
| 三条规则 + 六层涌现 | 0 (GEME) | 无时间 |
| τ 演化 + 预测 + doubt | 1 (G0) | GI¹=4 |
| 碰数检测 + 外置化 | 2 (G0') | GI²=16 |
| 跨代信息质量 | N (G0^N) | GI^N |

### 2.3 通信协议

```
深度 N → 深度 N+1：
  深度 N 的完整输出状态
  以 GI^(N+1) 间隔被深度 N+1 采样
  采样协议：arrow_output（深度 N 的 centroid + τ 状态）

深度 N+1 → 深度 N：
  不直接修改深度 N 的内部状态
  通过 bias_weight 调制——GABA 梯度模式
```

---

## 3. 与 BGM 的等价性

BGM 已经实现了深度 0 + 深度 1——EE 增加深度 2 及以上：

| BGM 组件 | 深度 | 角色 |
|----------|------|------|
| GEMENet 单元 | 0 | 棱镜 |
| G0 | 1 | 观察者 |
| GI=4 | 1→2 边界 | 时间解耦常数 |
| τ 分化 +49% | 1→2 效果 | 解耦效果 |
| g0_interval=4 | 1 采样间隔 | 最优采样频率 |
| G 句子实验 | 2 碰数检测 | EE 新增 |

---

## 4. 重构计划

### 4.1 第一阶段：Geruon 核心升级

| 任务 | 当前 | 目标 |
|------|------|------|
| τ 距离进入合并条件 | `vec_dist < δ_eff` | `vec_dist < δ_eff AND |τ_a − τ_b| < ε` |
| τ 距离进入共现权重 | `count += 1` | `count += 1 − |τ_a − τ_b|` |
| τ 距离进入剪枝排序 | `weight − age×γ` | `weight − age×γ − |τ_current − τ_frame|×γ_τ` |
| depth 参数 | 无 | Geruon.__init__ 增加 depth=N, 观测间隔=GI^N |
| 输入源切换 | 从环境输入 | depth>0 时从 arrow_output(depth-1) 输入 |

### 4.2 第二阶段：递归深度 1——G0 层

直接继承 BGM 的 G0 实现（bgm_core.py），增加 τ 演化和 doubt 检测。

### 4.3 第三阶段：递归深度 2——G0' 层

碰数检测、外置化记忆、信息质量测量。G 句子实验在此层复现。

---

## 5. 当前代码的处置

| 代码 | 处理 |
|------|------|
| `geruon.py` | 保留全部功能；增加 depth 参数 + τ 距离操作 + 输入源切换 |
| `bgm_core.py` G0 | 继承——作为 Geruon 递归实例（depth=1, GI=4） |
| 新文件 `ee_g0prime.py` | 深度 2 G0' + 碰数（depth=2, GI²=16） |

---

## 6. 实验验证

| 测试 | 深度 | 预期 |
|------|------|------|
| s1_demo.py | 0 | 与原始 GEME 一致 |
| 螃蟹卡农 + G 句子 | 0+1 | τ 不受棱镜干扰 |
| E7 复现 | 0+1+2 | 碰数可触发 |

---

## 7. 时间尺度递归的定义性公式

EE 的完整架构：

```
深度 0:  GEME（帧经济，无时间）
深度 1:  G0（τ 演化，GI=4 采样）
深度 2:  G0'（碰数检测，GI²=16 采样）
深度 N:  G0^(N)（信息质量在 GI^N 上积累）
```

这不是 EE 的新设计——这是 BGM 核心发现的显式化。EE 只是把 BGM 做了一次：重复。

---

*此文档记录 2026-05-18 早上的架构理解转折点。Geruon 的错误不是功能问题——是层归属问题。修复方法不是重新实现——是正确分配。*
